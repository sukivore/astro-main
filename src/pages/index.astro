---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!DOCTYPE html>
<html lang="en">
	<meta charset=utf-8><meta content="initial-scale=1,width=device-width,viewport-fit=cover" name=viewport><meta name=color-scheme content="dark light">
	
		
		<style>
				
	:root {
  --margin: 2.4rem;
  --margin--mobile: 1.8rem;

  --font-size--header: 1.8rem;
  --font-size--header--mobile: 1.6rem;
  --font-size--header--print: 1.2rem;
  
  --line-height: calc(1.3 * var(--font-size--header));

  --font-size--nav: 3.2rem;
  --font-size--nav--mobile: 2.2rem;

  --typography--size: var(--font-size--header);

  --color--background: #f8f9fa;
  --color--gray: #EFEFEF;
  --color--content: #00F;
  --color--content--secondary: #000;

  --skew: 1.5deg;
  --skew--mobile: .75deg;

  --schedule--grid: minmax(0, 1fr) minmax(0, 8fr) minmax(0, 2fr);
  --schedule--cell--padding: calc(.5 * var(--margin));
  --schedule--cell--padding--mobile: calc(.25 * var(--margin));
  --schedule--grid--mobile: minmax(0, 1fr) minmax(0, 2fr) minmax(0, 1fr);
}

@media screen and (max-width: 820px) {
  :root {
    --font-size--header: var(--font-size--header--mobile);
    --font-size--nav: var(--font-size--nav--mobile);
    --skew: var(--skew--mobile);
    --margin: var(--margin--mobile);
    --schedule--grid: var(--schedule--grid--mobile);
    --schedule--cell--padding: var(--schedule--cell--padding--mobile);
  }
}

@media print {
  :root {
    --font-size--header: var(--font-size--header--print);
  }
}

/* Reset */
* {
  box-sizing: border-box;
}

html {
  font-size: 62.5%;
  background-color: var(--color--background);
  color: var(--color--content--secondary);
}

header {
  font-family: 'Times New Roman', serif;
  font-size: var(--font-size--header);
  line-height: var(--line-height);
}

html, 
h1, h2, h3, p, 
ul, li,
dl, dt, dd, figure {
  margin: initial;
  padding: initial;
}

ul {
  list-style: none;
}

ul li::before {
  content: '→';
  width: 2.5rem;
  margin-left: -2.5rem;
  display: inline-block;
}

ul li {
  margin-left: 2.5rem;
}

.ui {
  font-family:'Arial', sans-serif;
}

.center {
  text-align: center;
}

/* Nav */
select{
	font-size: var(--font-size--nav);
	left: var(--margin);
  top: var(--margin);
  z-index: 1;
  position: fixed;
	cursor: pointer;
  background-color: white;
  color: var(--color--content);
}
	
	/* Content */
.layout-wrapper {
  overflow-y: hidden;
  max-width: 820px;
  margin-left: auto;
  margin-right: auto;
  padding-top: calc(3 * var(--margin));
  padding-bottom: calc(0.1 * var(--margin));
}

.content {
  padding: calc(0.1 * var(--margin));
  margin: calc(0.1 * var(--margin));
  background-color: var(--color--gray);
  transform: skew(var(--skew), var(--skew));
  color: var(--color--content);

  column-count: var(--column--count, 2);
  column-gap: var(--margin);
}</style>
			<head>
		
	</head>
	<body>
		
<main class=layout-wrapper>			
	<div class="content text">
    <h1>KAT ✦ YAN</h1>
    <p>Researcher, writer, internet explorer.<br><br></p>
    <ul>
    
    <h2>elsewhere</h2><br>
    <p>online<br></p>
    <ul>
    <li><a href=https://http//read.cv/katyan>cv</a></li><li><a href="mailto:meta@narrative.technology" target="_blank">contact</a></li>
	    <li>Ξ langwidere.eth [f.k.a. localcarguy.eth]</li></ul> <p>✦</p>
 <p>offline<br></p>
 <ul>
 <li>+61 425 267 688</li><br>Naarm/Melbourne, Australia. <br>
I acknowledge the traditional custodians of the unceded lands of the <br>
Wurundjeri, Taungurung, and Gunaikurnai peoples on which I live. <br>
I pay my respects to their Elders past and present, and to the <br>
oldest continuing cultures on this planet that have nurtured <br>
these lands, rivers, mountains, and tides since time immemorial. <br><br><li>Relocating stateside 2024. </li></ul>
				
				</div>
			
		</main>
		<Footer />
	</body>
</html>
